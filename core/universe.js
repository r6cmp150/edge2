// core/universe.js — owned by neither engine. core/ never imports from engines/.
// Phase 1 stub. Will hold getUniverse({ session, strategy }) — the shared
// movers/most-actives/premarket-gap scan described in
// docs/warrior-engine-spec-v2.md Phase 1. Deliberately empty in Phase 0:
// STOCK_UNIVERSES/TICKERS stay in app.js untouched until Phase 1 lands.
